# GitHub Container Registry credentials
# Copy this file to ghcr.io and fill in your credentials
# DO NOT commit ghcr.io to Git!
#
# Instructions:
# 1. Copy this file: cp deployment/ghcr.io.example deployment/ghcr.io
# 2. Edit deployment/ghcr.io and fill in your GitHub username and token
# 3. Set secure permissions: chmod 600 deployment/ghcr.io
#
# Creating a Personal Access Token (PAT):
# 1. Go to: https://github.com/settings/tokens
# 2. Click "Generate new token" -> "Generate new token (classic)"
# 3. Give it a descriptive name (e.g., "GHCR Push Token")
# 4. Select expiration (recommended: 90 days or custom)
# 5. Required scopes:
#    - write:packages (to push images)
#    - read:packages (to pull images)
# 6. Click "Generate token"
# 7. Copy the token immediately (it starts with ghp_)
# 8. Paste it below as GHCR_TOKEN
#
# Your GitHub username
export GHCR_USERNAME="your_github_username"

# Your GitHub Personal Access Token (PAT)
# Token must start with "ghp_"
export GHCR_TOKEN="ghp_your_token_here"
