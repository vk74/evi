# EV2 MVP Configuration
# Copy this file to .env and change the passwords

# =============================================================================
# REQUIRED: Change these passwords!
# =============================================================================

# PostgreSQL admin password (REQUIRED - change this!)
POSTGRES_PASSWORD=your_admin_password_here

# Application database password (REQUIRED - change this!)
APP_DB_PASSWORD=your_app_password_here

# =============================================================================
# OPTIONAL: Custom settings (usually no need to change)
# =============================================================================

# GitHub repository (change if you forked the project)
GITHUB_REPOSITORY=your-org/ev2

# Version to use (latest, v1.0.0, etc.)
VERSION=latest

# Database settings
POSTGRES_DB=maindb
POSTGRES_USER=postgres
POSTGRES_PORT=5432

# Application ports
API_PORT=3000
FRONTEND_PORT=8080

# Frontend API URL (change if backend runs on different host)
FRONTEND_API_URL=http://localhost:3000

# =============================================================================
# SECURITY WARNING
# =============================================================================
# 
# 1. ALWAYS change POSTGRES_PASSWORD and APP_DB_PASSWORD
# 2. Use strong passwords (12+ characters, mixed case, numbers, symbols)
# 3. Never commit .env file to version control
#
