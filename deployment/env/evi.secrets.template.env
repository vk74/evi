# Version: 1.0.0
# Purpose: Production deployment secrets template for evi (SECRET values).
# Deployment file: evi.secrets.template.env
# Logic: User edits this file locally; `evictl` reads it and creates Podman secrets. Do NOT commit real secrets.
#
# Changes in v1.0.0:
# - Initial secrets contract for DB password, JWT RS256 private key, and optional manual TLS keypair
#

# =========================
# Database secret(s)
# =========================

# PostgreSQL superuser password (postgres)
EVI_POSTGRES_PASSWORD=change_me_strong

# Admin DB password for 'admin' user (human access)
EVI_ADMIN_DB_PASSWORD=

# Application DB password for app user (app_service).
# Required for backend connectivity.
EVI_APP_DB_PASSWORD=

# =========================
# JWT signing key (RS256)
# =========================

# Option A (recommended): provide path to an existing RSA private key PEM (will be imported as a secret)
EVI_JWT_PRIVATE_KEY_PEM_PATH=

# Option B: inline PEM content (multiline). If set, it will be used to create the secret.
# WARNING: do not store this in git; keep it only on the server.
EVI_JWT_PRIVATE_KEY_PEM_INLINE=

# Option C: generate a new key during `evictl init`
EVI_JWT_GENERATE_KEY=true

# =========================
# TLS certificate (manual mode only)
# =========================

# If EVI_TLS_MODE=manual, provide certificate and key paths.
EVI_TLS_CERT_PATH=
EVI_TLS_KEY_PATH=


