/**
 * @file adminStateTypesOld.ts
 * 
 * Типы для хранилища состояния модуля администрирования.
 */

export type AdminModuleState = {
    activeSubModule: string;
    drawerMode: 'auto' | 'opened' | 'closed';
    previousModule: string | null;
    activeSection: string;
    previousSection: string | null;
    activeCatalogSection: string;
    previousCatalogSection: string | null;
    activeUserSection: string;
    previousUserSection: string | null;
    activeUserSubModule: string | null;
  }
  
  export interface AdminStateActions {
    setActiveSubModule: (module: string) => void;
    setDrawerMode: (mode: 'auto' | 'opened' | 'closed') => void;
    returnToPreviousModule: () => void;
    setActiveSection: (sectionId: 'active-services' | 'all-services') => void;
    returnToPreviousSection: () => void;
    setActiveCatalogSection: (sectionId: 'settings' | 'visualization' | 'access') => void;
    returnToPreviousCatalogSection: () => void;
    setActiveUserSection: (sectionId: 'users' | 'groups') => void;
    returnToPreviousUserSection: () => void;
    setActiveUserSubModule: (subModule: 'SubModuleUserEditor' | 'SubModuleGroupEditor' | null) => void;
    resetActiveUserSubModule: () => void;
    resetUserModuleState: () => void;
    resetCatalogModuleState: () => void;
    resetServiceModuleState: () => void;
    resetAllState: () => void;
  }